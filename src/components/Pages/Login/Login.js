import React from 'react';
import Navbar from '../../SharedPage/Navbar';

const Login = () => {
    return (
        <div>
            <Navbar></Navbar>
            <h2>Login</h2>
        </div>
    );
};

export default Login;