import React from 'react';
import Navbar from '../SharedPage/Navbar';

const Blogs = () => {
    return (
        <div>
            <Navbar></Navbar>
            Blogs
        </div>
    );
};

export default Blogs;